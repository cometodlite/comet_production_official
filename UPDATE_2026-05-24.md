# COMET PRODUCTION 웹사이트 업데이트 노트

**날짜**: 2026년 5월 24일
**배포 상태**: ✅ Vercel 프로덕션 배포 완료 (● Ready)
**커밋**: `4aec944` → `63c069b` → `a512d53` (main)

---

## 1. 평가 시스템 확장 (`4aec944`)

### 시험 일정 관리
- 등록일/실전일을 **이사회 페이지에서 직접 설정** 가능 (기존 하드코딩 4·14·24 / 5·15·25일은 기본값으로 유지)
- 트랙별(예: 개발/엔터) 일정 오버라이드 지원
- **실전 시험 시작 시각: 12:00 KST** — 실전일 오전에는 "오늘 12:00에 시험이 시작됩니다" 안내 + 카운트다운, 등록일에는 "내일 12:00에 시험이 시작됩니다" 표시

### 실전 시험 응시 화면
- **문항 네비게이션 맵**: 화면 상단에 고정되는 문항 번호 바 — 응답 완료(파랑)/미응답(회색)/현재 보는 문항(테두리) 표시, 클릭 시 해당 문항으로 이동, 진행률 바 포함
- **응답 시간 패턴 기록**: 문항별 최초 응답 시각을 자동 기록

### 이사회(채점) 페이지
- **합불 결정**: 채점 후 ✓합격/✗불합격 확정 + 결정 사유 메모, "미결정"으로 되돌리기 가능
- **부정행위 감지 강화**: 10초 미만 응답 시 ⚡빠른응답 경고 배지, 문항별 응답 시간 시각화
- **평가 대시보드**: 제출 통계, 최근 14일 제출 활동 그래프
- **문제 세트 관리 UI** (`/staff/board/questions`): 객관식·서술형 문제 추가/편집/삭제, 세트 단위 관리, 세트 활성화(활성 세트가 실전에 출제), 트랙별 세트 지정
- CSV 내보내기에 응답시간·합불 컬럼 추가

### 응시자 화면
- **성적 확인 페이지** (`/evaluation/history`): 본인 제출 이력 + 합불 결과 확인
- 합불 배지: ✓합격(초록) / ✗불합격(빨강) / 채점 대기 중(노랑)

### 공개 결과 조회
- **`/results` 페이지 신설**: 로그인 없이 응시자 이름으로 합불 결과 조회
- 홈페이지에 "04 / EVALUATION RESULTS" 섹션 + 네비게이션 바에 "결과 조회" 링크 추가

### 이메일 알림 (Resend)
- 새 답안 제출 → 이사회에 알림 메일
- 합불 확정 → 응시자에게 결과 메일
- ⚠️ **작동하려면 Vercel 환경 변수 필요** (아래 "남은 설정" 참고)

### 개발용 도구
- **기간 시뮬레이터** (`/dev/period-test`): 날짜(1~31일)·시각(00~23시)을 골라 해당 시점의 평가 기간(등록/실전/연습)과 카운트다운, 화면 동작을 미리 확인

---

## 2. 디자인 정제 — Apple 스타일 (`63c069b`)

### 전역 타이포그래피·디테일
- 본문 자간을 Apple 표준(-0.022em)으로 정밀화, 커닝·리거처 활성화
- 헤드라인 균형 줄바꿈(text-wrap: balance), 숫자 고정폭 정렬(tabular-nums)
- 얇고 단정한 커스텀 스크롤바, 텍스트 선택 색상, 키보드 포커스 링
- 모션 민감 사용자 배려(prefers-reduced-motion) — 접근성 향상

### 버튼 시스템 통일
- `.btn-primary` / `.btn-ghost` — Apple식 알약(pill) 버튼, 부드러운 호버 리프트
- 브랜드 색 변형: **골드**(Entertainers) / **블루**(Develops)
- 홈·채용·문의·슬라이드 CTA 등 사이트 전체 버튼이 한 시스템으로 통일

### 모션 언어 통일
- 홈 Hero: 타자기+깜빡이는 커서 → **부드러운 blur 페이드인**
- Develops: RGB 글자깨짐 글리치 제거, 키워드 타이핑 → **단어 크로스페이드**
- 두 핀(pinned) 섹션이 같은 모션 언어 공유

### 아이콘 시스템
- 이모지 UI 아이콘(🎤🌟🚀🎮📦🔧📜🛰️🔍😶) → **모노크롬 라인 SVG 아이콘 10종** 신규 제작
- 브랜드 컬러 타일 안에 배치해 Apple 기능 소개 스타일로 통일

### 기타
- Navbar: 활성 메뉴에 부드럽게 따라오는 점 인디케이터, 글자 굵기 변화로 인한 미세 흔들림 제거
- Contact 폼: 입력 필드 포커스 상태를 사이트 디자인 시스템과 통일
- About: 중복 헤딩 정리("핵심 가치/핵심 가치" → "CORE VALUES / 우리가 믿는 가치")
- 투명도 표기 정합화(`white/8` → `white/[0.08]`) — 전 페이지
- 빌드를 막던 린트 에러 8건 수정

---

## 3. 배포 버그 수정 (`a512d53`)

- 첫 배포가 **빌드 실패**(● Error)로 반영되지 않았던 문제 해결
- 원인: `/dev/period-test` 페이지가 `useSearchParams()`를 Suspense 경계 없이 사용 → 프로덕션 프리렌더링 에러
- Suspense 래퍼로 분리 후 로컬 프로덕션 빌드 전체 통과 확인 → 재배포 성공

---

## ⚠️ 남은 설정 (수동 작업 필요)

| 항목 | 위치 | 내용 |
|------|------|------|
| 이메일 알림 | Vercel → Settings → Environment Variables | `RESEND_API_KEY`, `EMAIL_FROM`, `BOARD_NOTIFICATION_EMAIL` 추가 (없으면 이메일만 건너뛰고 정상 작동) |
| 사원 가입 코드 | Vercel 환경 변수 + 로컬 `.env.local` | `STAFF_CODE_ENTERTAINERS` 미설정 — 김도영님 가입 전 코드 값 결정 필요 |

---

## 참고

- 저장소: https://github.com/cometodlite/comet_production_official
- `/dev/period-test`는 개발용 페이지이지만 프로덕션에서도 접근 가능합니다(민감 정보 없음, 클라이언트 시뮬레이션만). 숨기고 싶으시면 말씀해주세요.
